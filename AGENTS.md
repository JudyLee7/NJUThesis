# Project Notes for Codex

- Always compile from the repository root (`/Users/lijiaqian/Desktop/NJUThesis`) using `docs/njuthesis-sample.tex` as the main file.
- After every LaTeX compilation, rename the generated `njuthesis-sample.pdf` to a timestamped thesis file named `<YYYYMMDD_HHMM>_毕设论文.pdf` in the repository root. Do not delete previous timestamped PDF outputs.
- After every LaTeX compilation, delete auxiliary compilation artifacts such as `.aux`, `.bbl`, `.bcf`, `.blg`, `.fdb_latexmk`, `.fls`, `.log`, `.out`, `.run.xml`, `.thm`, `.toc`, `.xdv`, and SyncTeX files. Keep timestamped PDFs and source files.
