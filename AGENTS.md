# Project Notes for Codex

- Always compile from the repository root (`/Users/lijiaqian/Desktop/NJUThesis`) using `docs/njuthesis-sample.tex` as the main file.
- After every LaTeX compilation, delete auxiliary compilation artifacts such as `.aux`, `.bbl`, `.bcf`, `.blg`, `.fdb_latexmk`, `.fls`, `.log`, `.out`, `.run.xml`, `.thm`, `.toc`, `.xdv`, and SyncTeX files. Keep the generated PDF and source files.
